# Atlantic Hub - Netlify Environment Variables Reference

**Last updated:** 2026-05-17

**What this is:** The complete list of environment variables the atlantic-hub
Next.js code reads at runtime. Generated by scanning every `process.env.*`
reference in the codebase, so this is authoritative not guessed.

**Where to set them:**
Netlify dashboard > atlantic-hub site > Site configuration > Environment variables

---

## VARIABLES YOU ALREADY SET (existing in Netlify before today)

These the code has been using for weeks. Don't change unless something breaks.

### Database connections (HostGator MySQL)

| Variable | Purpose |
| --- | --- |
| DB_HOST | HostGator MySQL host |
| DB_PORT | Usually 3306 |
| DB_USER_AV / DB_PASS_AV / DB_NAME_AV | Atlantic and Vine database |
| DB_USER_HH / DB_PASS_HH / DB_NAME_HH | HunterHoney database |
| DB_USER_EBW / DB_PASS_EBW / DB_NAME_EBW | Events by Water database |
| DB_USER_PLATFORM / DB_PASS_PLATFORM / DB_NAME_PLATFORM | Atlantic Hub platform database |

### Authentication and security

| Variable | Purpose |
| --- | --- |
| JWT_SECRET | Random string used to sign auth tokens |
| JWT_ISSUER | Issuer string, usually atlantic-hub |
| OWNER_BOOTSTRAP_EMAIL | Your owner login email |
| OWNER_BOOTSTRAP_PASSWORD_HASH | bcrypt hash of owner password |
| EMAIL_ENCRYPTION_KEY | Random key for encrypting stored emails |
| IP_SALT | Random string for hashing IPs in rate-limit logs |

### External APIs already wired

| Variable | Purpose |
| --- | --- |
| HUNTER_API_KEY | hunter.io email verification |
| APOLLO_API_KEY | apollo.io people and company search |

### Cron and webhooks

| Variable | Purpose |
| --- | --- |
| ENRICHMENT_CRON_SECRET | Shared secret for the daily 6 AM Hunter enrichment cron |
| NETLIFY_FORMS_WEBHOOK_SECRET | If you wire Netlify Forms to a webhook |

---

## VARIABLES ADDED TODAY (need to be in Netlify)

These are the variables MY CODE from this session reads. If they aren't set
in Netlify, the new features will return a clear error instead of crashing.

| Variable | Purpose | Source page or feature |
| --- | --- | --- |
| GOOGLE_PLACES_API_KEY | Google Cloud API key with Places API (New) enabled | /admin/av/discover (Google Places tab) |
| APIFY_API_TOKEN | apify.com API token | /admin/av/discover (Instagram tab) |
| OPENAI_API_KEY | platform.openai.com API key | Generate Social Content button on every lead, future AI outreach |
| XAI_API_KEY | console.x.ai API key for Grok Imagine | Commercials tab on every lead (per-lead AI image + video generation, schema 011) |

**Per Val 2026-05-17:**
- GOOGLE_PLACES_API_KEY: she already set this in Netlify
- APIFY_API_TOKEN: she already set this in Netlify
- OPENAI_API_KEY: adding this now

---

## CLIENT PORTAL (added 2026-05-17c)

Both optional. Sensible defaults are baked in, so the portal works without
either of them being set; you only need them if you want to override.

| Variable | Purpose | Default |
| --- | --- | --- |
| MAGIC_LINK_BASE_URL | Origin used in the magic-link URLs we log to Netlify function output. Set this to the public origin of atlantic-hub so the link Val pastes to clients is fully qualified. | "" (empty = relative path) |
| PORTAL_ALLOWED_ORIGINS | Comma-separated origin allowlist for the cross-origin `/api/client/intake` endpoint. | https://atlanticandvine.netlify.app,https://atlanticandvine.com,https://www.atlanticandvine.com |

Recommended Netlify values:
- MAGIC_LINK_BASE_URL = https://atlantic-hub.netlify.app
- PORTAL_ALLOWED_ORIGINS = (leave unset to use the default allowlist)

No new secrets. The portal reuses the existing JWT_SECRET and JWT_ISSUER
for its session cookies.

---

## SYSTEM VARIABLES (Netlify sets these automatically)

You don't touch these. Listed here so future-you knows what they are.

| Variable | Set by | What |
| --- | --- | --- |
| URL | Netlify | Production site URL |
| HOST | Netlify | Hostname |
| NODE_ENV | Netlify | "production" |
| NODE_UNIQUE_ID | Netlify build | Build identifier |
| PATH | OS | Shell PATH |
| NODE_DISABLE_COLORS | Build runtime | Color flag |
| BOOK_LANG | App | Language flag for HH book feature |

---

## HOW TO ADD A VARIABLE TO NETLIFY

1. Go to netlify.com and sign in
2. Click the atlantic-hub site
3. Site configuration > Environment variables
4. Click "Add a variable" > "Add a single variable"
5. Key: the variable name (case-sensitive, all caps)
6. Value: paste the secret
7. Scopes: leave at "All scopes" (production, deploy previews, branch deploys)
8. Save
9. Netlify rebuilds automatically OR click "Trigger deploy" > "Clear cache and deploy site"

---

## HOW TO TEST A VARIABLE IS SET CORRECTLY

After deploy, hit one of the API endpoints that uses it. If the variable
is missing or wrong, my code returns a 503 with a clear error like:
- "GOOGLE_PLACES_API_KEY not configured in Netlify env vars"
- "APIFY_API_TOKEN is not set in Netlify environment variables"
- "OPENAI_API_KEY not configured in Netlify env vars"

So you don't need to guess. If a feature breaks, the error tells you which
variable is missing.

---

## HOSTGATOR-SIDE VARIABLES (different system)

The PHP backend at api.atlanticandvine.com uses HostGator's own environment.
Those are set in cPanel under "Software > MultiPHP INI Editor" or in the
PHP files directly. NOT in Netlify. The two systems don't share env vars.

If you need a list of HostGator-side variables, look at the .php files in
AV_livewebsite/api/ and grep for getenv() and $_ENV.
