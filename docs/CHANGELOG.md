# Atlantic Hub Changelog

One line per shipped session. Most recent first. Conductor (Cowork Claude)
appends after each session reports a commit hash.

Format: `YYYY-MM-DD  <commit>  <session name>  --  <one-line scope>`

---

## 2026-05-21

- `<pending>`  Clay Webhook  --  schema/012 clay_enrichment_log (idempotent, information_schema-guarded CREATE). lib/clay/webhook.ts (verifyClaySecret constant-time X-Webhook-Secret check + parseClayPayload fuzzy field extraction over user-defined Clay column names, envelope-aware data/row/payload/root + payloadIsUseful floor). lib/clay/discoverer.ts (ingestClayRow: cross-source dedup via findExistingLead, fill-missing-only on existing leads -- never overwrites real email/phone/name, placeholder-email aware -- or INSERT new source_type=api lead with inferred target_business, fires scoreAndAuditLeadBackground + logEvent lead.enriched_clay / lead.created, writes clay_enrichment_log row, never throws). POST /api/admin/av/integrations/clay-webhook (NO guardAdminRequest -- shared-secret auth for Clay servers; 401 bad secret, 400 invalid payload, 429 in-memory rate limit 100/min/clay_table_id, 200 with outcome+leadId). /admin/av/integrations/clay status page (owner+staff, copyable webhook URL via CopyButton client component, setup steps, secret-set banner, schema-missing hint, outcome distribution all-time + 7d, last 50 rows). docs/ENV_VARS_REFERENCE.md documents CLAY_WEBHOOK_SECRET. Pure additive, no existing files modified. Verified tsc --noEmit exit 0; full next build runs on Netlify deploy (sandbox 45s/command limit blocked the local full build). Wine-themed gamification ideas from the kickoff chat parked to docs/CLAUDE_KICKOFF_VINE_GAMIFICATION_FUTURE.md (clashes with locked operator aesthetic; belongs on the AV brand surface).

## 2026-05-19

- `<pending>`  Sales Team mega-ship (ships 2 to 5 of VP-of-Sales rollout, one commit)  --  schema/019 adds pain_point_profile JSON + pain_extracted_at, assigned_to_user_id, handed_to_owner_at, wake_at_date, parked_reason, extends lead_status enum (adds nurture / not_now / referred / case_study), new call_log table (lead_id + user_id + outcome enum + duration + notes + called_at). lib/ai/pain_extractor.ts (OpenAI gpt-4o-mini call returns structured profile with primary_pain + urgency_signal + decision_maker_proximity + budget_signal + timing_signal + last_objection_seen + conversation_starters + do_not_say). lib/sales/pipeline_value.ts (computes Live / Hot / Warm / Nurture $ rollups + top opportunity). lib/leads/lifecycle.ts (transitionLeadStatus + maybeWakeOnEngagement + runDateBasedWakeSweep + isParked / isTerminal / isValidStatus + defaultWakeDate). engagement_score.ts EVENT_WEIGHTS extended with call outcomes (connected +4, meeting_booked +12, not_interested -3, follow_up +2) and lifecycle events (handed_to_owner +8, lifecycle.case_study +4, woken_by_behavior +5). API: /api/admin/av/leads/[audit_id]/calls (GET + POST), /lifecycle (POST), /assign (POST handles assignToUserId + handToOwner). /api/admin/av/pipeline-value, pain-sweep, nurture-wake. /api/admin/users for assignment dropdown. Netlify crons: pain-extract-cron daily 08:00 UTC, nurture-wake-cron daily 07:30 UTC, both reuse ENRICHMENT_CRON_SECRET. UI: PainPointCallout (gradient callout above tabs), CallLogPanel + new Calls tab on lead detail, LifecycleControls in Identity tab replaces stage select with extended-enum chips + wake-date / parked-reason form, AssignmentControl in header (Assign-to-me dropdown + Hand-to-owner toggle), PipelineValueCard at top of /admin/av (animated $ rollup, refresh every 30s, top-opportunity callout), ConversionConfetti fires on lifecycle transition to "converted" with celebratory toast. /admin/av sales-team filter chips: My leads / Unassigned / Owner queue. Leads list + detail APIs surface every new field. Leads list sort keys "score" / "fit" / "engagement" all supported.
- `<pending>`  Living Score (ship 1 of 5, VP-of-Sales rollout)  --  schema/018 adds ai_engagement_score (signed int), ai_combined_score (the visible 0-100), score_history JSON, engagement_score_updated_at, idx_combined_score, plus backfill so existing scored leads default to combined == fit. lib/ai/engagement_score.ts module with EVENT_WEIGHTS table covering operator-interest, outreach lifecycle, social, client-portal, and manual-stage signals (positive reply +25, unsubscribe -50, etc), applyEngagementSignal + applyEngagementSignalBackground + recomputeCombinedForLead + readScoreHistory. lib/events/log.ts now fires the engagement signal in the background on every successful event with a leadId (permissive: unknown event types ignored, so other sessions can emit new events without coordinating). lib/ai/score_and_audit.ts calls recomputeCombinedForLead after Re-score so the dashboard reflects the new fit immediately. APIs: leads list + lead detail return ai_engagement_score / ai_combined_score / engagement_score_updated_at / score_history; sort key "score" now sorts on combined, new "fit" + "engagement" sort keys exposed for power users. UI: AvLeadsTable score cell shows combined number plus a directional engagement delta badge, lead detail header shows the combined number in AnimatedScoreReveal, new ScoreSparkline component renders score-history trend on AI Scoring tab, new EngagementCard tile + Fit/Combined/Band cards in a four-up grid. LeadOfTheDay big number now shows combined with a delta indicator.
- `393d684`  Cosmetic Polish (round 2)  --  app/globals.css design-token contract block + shared .ah-action-sparkle class (replaces per-component styled-jsx). components/LeadOfTheDay.tsx widened to 3-tier eligibility fallback (today hot/warm -> this week hot/warm -> top overall >=60) so the brand banner stays visible most days instead of going silent. app/admin/av/[audit_id]/RescoreButton.tsx refactored onto the shared class + accessibility additions (aria-label, aria-live, focus-visible ring). docs/COSMETIC_BASELINE.md updated to document both the shared sparkle pattern markup template and the lead-of-the-day fallback tiers. OutreachPanel.tsx (other session) still inlines a duplicate copy of the sparkle CSS -- nudge sent for next-pass cleanup.

---

## 2026-05-18

- `<pending>`  Email Outreach Automation  --  schema/014 outreach_mailboxes + outreach_campaigns + outreach_messages + outreach_replies + outreach_send_log. lib/email/ driver layer (HostGator SMTP via nodemailer, Microsoft Graph OAuth, Gmail API OAuth) with AES-256-GCM credential encryption (lib/email/encrypt.ts), router + per-mailbox + per-tier daily caps. lib/ai/outreach_drafter.ts (audit-grounded personalized drafts, plural voice, no-cost-leak prompts), lib/ai/reply_classifier.ts (positive/interested/neutral/negative/autoresponder/unsubscribe with heuristic fast-path), lib/email/send_pipeline.ts (approve -> driver send -> log -> auto-advance lead_status new->contacted on send / contacted->qualified on positive reply / ->lost on unsubscribe). API routes: mailboxes CRUD + test + Microsoft/Google OAuth start/callback, campaigns CRUD with pause/resume, draft generator per lead, messages list + approve + reject, replies list, replies poll (cron + manual). Netlify cron outreach-poll-cron.mts (every 15 min). UI: /admin/av/outreach overview with live polling + once-per-day positive-reply celebration, /admin/av/outreach/new campaign wizard, /admin/av/outreach/[campaign_id] detail + edit + pause, /admin/av/outreach/mailboxes connect HostGator + OAuth handoff. OutreachPanel tab on every lead with sparkle Generate button + audit-excerpt callout. Sidebar Outreach link. Per val: NO third-party cold-email SaaS (Instantly not used) -- multi-driver architecture lets the operator and each client send from mailboxes they already own.
- `<pending>`  Grok Imagine  --  schema/011 grok_imagine_assets + grok_imagine_log, lib/grok/imagine.ts (xAI image + async video client), lib/grok/discoverer.ts (per-lead orchestrator with audit-driven prompt), POST + GET + DELETE routes at /api/admin/av/leads/[audit_id]/commercial[/[asset_id]], CommercialPanel.tsx + new "Commercials" tab on lead detail. Pricing surfaces intentionally untouched per Val (decision pending).

## 2026-05-17

- `e8ee628`  Auto-Scoring + Events  --  schema/010 system_events table, lib/events/log.ts, lib/ai/score_and_audit.ts, fire-and-forget scoring on every lead insert across Apollo / Google Places / Instagram / CSV / scrape, /admin/events observability page, owner Re-score button, daily 07:00 UTC score-sweep cron, Sidebar link
- `50bc550`  Client Portal  --  schema/009 client_users table, magic-link auth, /client/dashboard, /client/audit, /client/login, /client/set-password pages, middleware.ts client-route routing, lib/auth/client-*, lib/client-portal/tiers.ts

## Earlier shipped (pre-changelog)

- 2026-05-17  schema/008 target_business + normalized_domain + archive index
- 2026-05-16  schema/007 Apollo integration + apollo_search_log
- 2026-05-16  schema/006 enrichment + Hunter credit log
- 2026-05-16  schema/005 EBW detail tables
- 2026-05-12  schema/004 AV detail tables (v1 through v4)
- 2026-05-11  schema/001-003 platform + HH detail + seed
